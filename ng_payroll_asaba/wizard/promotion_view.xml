<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_tree_employee_promotion" model="ir.ui.view">
            <field name="name">view.tree.employee.promotion</field>
            <field name="model">employee.promotion</field>
            <field name="arch" type="xml">
                <tree string="">
                    <field name="dept_sector_id"/>
                    <field name="state"/>
                </tree>
            </field>
        </record>

        <record id="view_form_employee_promotion" model="ir.ui.view">
            <field name="name">view_form_employee_promotion</field>
            <field name="model">employee.promotion</field>
            <field name="arch" type="xml">
                <form string="Employee Promotion">
                    <header>
                        <button name="submit" string="Submit" type="object" class="oe_highlight"
                                attrs="{'invisible': [('state','!=', 'draft')]}"/>
                        <button name="approve" string="Approve" type="object" class="oe_highlight"
                                attrs="{'invisible': [('state', '!=', 'confirm')]}"/>
                        <button name="cancel" string="Cancel" type="object" class="btn-danger"
                                attrs="{'invisible': [('state', '!=', 'confirm')]}"/>
                        <button name="set_draft" string="Set to draft" type="object" class="btn-default"
                                attrs="{'invisible': [('state', '!=', 'cancel')]}"/>
                        <field name="state" statusbar_visible="1" widget="statusbar" readonly="1"/>
                    </header>
                    <sheet string="Employee Promotion">
                        <group>
                            <group>
                               <field name="dept_sector_id"/>
                            </group>
                            <field name="employee_ids"/>
                        </group>
                        <div class="oe_left oe_button_box" name="button_box">
                            <button name="do_promotion" string="Do Promotion" type="object" class="oe_stat_button"
                                    icon="fal fa-tasks"
                        attrs="{'invisible': [('state', '!=', 'approve')]}"/>

                        </div>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids" widget="mail_followers" />
                        <field name="message_ids" widget="mail_thread" />
                    </div>
                </form>
            </field>
        </record>

        <!-- Employee Promotion Action -->
        <act_window
                id="action_employee_promotion"
                name="Employee Promotion"
                res_model="employee.promotion"
                view_mode="tree,form"
                target="current"
                />

        <!-- Promotion menu -->
        <menuitem
                id="menu_employee_promotion"
                name="Promotion"
                parent="ng_payroll_asaba.menu_employee_increment_promo"
                action="action_employee_promotion"
                sequence="0"
        />
    </data>
</odoo>